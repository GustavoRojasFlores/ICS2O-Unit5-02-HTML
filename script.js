document.getElementById("btn").addEventListener("click", displayDate) // Assigns an event listener with a function to the button so it can run said function when pressed.

function displayDate() {
  alert("Sample Alert Text") // Displays an alert message saying "Sample Alert Text"
} // Creates a new function matching the one referenced in the addEventListener function.